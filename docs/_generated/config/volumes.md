# Volumes (autogenerated)

Named volumes használat.

| Volume | Used by |
|---|---|
| `central-logger-config` | central-logger → central-logger-config:/config |
| `central-logger-logs` | central-logger → central-logger-logs:/var/log |
| `central-logger-logs2` | central-logger → central-logger-logs2:/logs |
| `central-logger-work` | central-logger → central-logger-work:/work |
| `grafana_data` | grafana → grafana_data:/var/lib/grafana |
| `ldap_config` | openldap → ldap_config:/etc/ldap/slapd.d |
| `ldap_data` | openldap → ldap_data:/var/lib/ldap |
| `litellm-db` | litellm-db → litellm-db:/var/lib/postgresql/data |
| `loki_data` | loki → loki_data:/loki |
| `openwebui-data` | openwebui → openwebui-data:/app/backend/data |
| `postgres_data` | openwebui_db → postgres_data:/var/lib/postgresql/data |
| `promtail_data` | promtail → promtail_data:/tmp |
| `pwm-data` | pwm → pwm-data:/usr/share/pwm |
| `rag_db_data` | rag-db → rag_db_data:/var/lib/postgresql/data |
| `valkey_data` | valkey → valkey_data:/data |